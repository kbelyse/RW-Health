NODE_ENV=development
# In production, set to the built frontend folder (e.g. ../web/dist when starting from apps/api)
WEB_DIST=
PORT=4000
DATABASE_URL=file:./dev.db
JWT_SECRET=local-dev-only-replace-with-random-string-32chars!!
COOKIE_SECURE=false
CORS_ORIGIN=http://localhost:5173
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM="RW-Health Passport <noreply@example.local>"
