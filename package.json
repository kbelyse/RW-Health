{
  "name": "rw-health-passport",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "setup": "node scripts/setup-env.mjs",
    "dev": "concurrently -n api,web -c blue,green \"npm run dev -w @rw-health/api\" \"npm run dev -w @rw-health/web\"",
    "build": "npm run build -w @rw-health/shared && npm run build -w @rw-health/api && npm run build -w @rw-health/web",
    "build:render": "npm install --include=dev && npm run build",
    "db:push": "npm run db:push -w @rw-health/api",
    "db:seed": "npm run db:seed -w @rw-health/api",
    "start:prod": "npm run start:prod -w @rw-health/api"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "typescript": "^5.7.2"
  }
}
